/** Shared sidecar HTTP path constants (main proxy allowlists + renderer fetch). */

/** Inbound LXMF catch-up ring (`GET` with optional since_ts / since_seq / limit). */
export const RETICULUM_LXMF_RECENT_API_PATH = '/api/v1/lxmf/recent';
