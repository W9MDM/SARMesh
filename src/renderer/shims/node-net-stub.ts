/**
 * Browser/Electron-renderer stub for Node's `net`.
 * Listed as a rollup external but no actual renderer-path imports found.
 * Stubbed defensively to prevent a bare `import "net"` in the browser bundle.
 */
export default {};
