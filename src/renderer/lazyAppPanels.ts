import { lazy } from 'react';

export const ConnectionPanel = lazy(() => import('./components/ConnectionPanel'));
export const ChatPanel = lazy(() => import('./components/ChatPanel'));
export const NodeListPanel = lazy(() => import('./components/NodeListPanel'));
export const LogPanel = lazy(() => import('./components/LogPanel'));
